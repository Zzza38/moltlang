# RFC process

Open an issue labeled `rfc` before changing syntax, types, runtime behavior, IR contracts, or backend semantics. An accepted RFC must include motivation, exact semantics, examples, rejected alternatives, migration impact, and conformance tests. Implementation PRs link the accepted RFC and do not silently expand scope.
